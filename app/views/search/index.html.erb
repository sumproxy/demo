<script>
  $(function() {
    function log( message ) {
      $( "<div>" ).text( message ).prependTo( "#log" );
      $( "#log" ).scrollTop( 0 );
    }
 
    $( "#birds" ).autocomplete({
      autoFocus: true,
      delay: 10,
      source: "<%= search_index_path(:json) %>", // expands to "/search/index.json",
      minLength: 1,
      select: function( event, ui ) {
        log( ui.item ?
          ui.item.label + " id: " + ui.item.id :
          "Nothing selected, input was " + this.value );
      }
    });
  });
</script>
 
<div class="ui-widget">
  <label for="birds">Введите ФИО пациента: </label>
  <input id="birds" />
</div>
 
<div class="ui-widget" style="margin-top: 2em; font-family: Arial;">
  Результат:
  <div id="log" style="height: 200px; width: 300px; overflow: auto;" class="ui-widget-content"></div>
</div>